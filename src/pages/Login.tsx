import { Typography } from '@mui/material';

const Login = () => {
  return (
    <section className='login__section'>
      <Typography variant='h1' align='center' className='login__title'>
        Login
      </Typography>
    </section>
  );
};

export default Login;
